# Copy this file to .env and fill in the value after deploying the backend.
# .env is gitignored and must never be committed.

# The API Gateway URL output by `sam deploy` (copy from the Outputs section)
VITE_API_URL=https://xxxxxxxxxxxx.execute-api.us-east-1.amazonaws.com/stock
